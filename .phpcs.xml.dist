<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="Documentate WordPress Coding Standards" xsi:noNamespaceSchemaLocation="https://schema.phpcodesniffer.com/phpcs.xsd">
    <description>WordPress Coding Standards for the Documentate plugin.</description>

    <!-- Own plugin PHP only. `public/` is reserved for front-end code if added later. -->
    <file>admin</file>
    <file>includes</file>
    <file>documentate.php</file>
    <file>uninstall.php</file>

    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/admin/vendor/*</exclude-pattern>
    <exclude-pattern>*/assets/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/wp/*</exclude-pattern>
    <exclude-pattern>*/cloudflare-worker/*</exclude-pattern>

    <arg name="colors"/>
    <arg name="extensions" value="php"/>
    <arg name="parallel" value="8"/>
    <arg name="warning-severity" value="0"/>

    <config name="minimum_wp_version" value="6.1"/>

    <!-- Check the code really runs on the minimum PHP the plugin declares.
         Keep testVersion in step with "Requires PHP" in documentate.php and
         readme.txt, and with the php-version CI installs. -->
    <config name="testVersion" value="8.3-"/>
    <rule ref="PHPCompatibilityWP"/>

    <rule ref="WordPress"/>

    <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
        <properties>
            <property name="prefixes" type="array">
                <element value="documentate"/>
                <element value="DOCUMENTATE"/>
            </property>
        </properties>
    </rule>
</ruleset>
